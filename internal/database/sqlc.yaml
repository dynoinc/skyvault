version: "2"
sql:
  - engine: "postgresql"
    queries: "."
    schema: "migrations"
    gen:
      go:
        package: "database"
        out: "."
        sql_package: "pgx/v5"
        emit_pointers_for_null_types: true